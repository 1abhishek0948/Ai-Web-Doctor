"""Models for the accounts application.

Django's built-in User model is used; no custom user model is required for
Part 1. This module exists so future account-related models (e.g. user
preferences or scan usage tracking) have a clear home.
"""

from __future__ import annotations
